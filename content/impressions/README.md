# 手寫的那一層

一場活動一個檔案，檔名是**永久鍵 ID**：`043.md` 對應 ID 43。
`npm run impressions` 會把這裡的內容折進 `src/data/events.json`
（`npm run build` 也會自動跑），網站就吃得到。

```markdown
---
一句話: 兩團第一次在台灣同台，四月的大佳河濱。
票價: 搖滾區 3800 / 座位區 2800
售票狀況: 開賣 3 分鐘完售
---

心得寫在這裡。支援 **粗體**、*斜體*、`行內碼`、[連結](https://example.com)、
清單、`>` 引言、`---` 分隔線。

## 曲目
1. STAR BEAT!〜ホシノコドウ〜
2. 天下トーイツ A to Z☆
安可
Returns

## 場刊
聲優訪談
台北公演特別寫真
```

## 哪些欄位寫在這裡

**前置資料**（一行一個）：`一句話`、`簡介`、`票價`、`售票狀況`、`主視覺`、`系列`

**`## 小標`**（一整串的）：`曲目`、`周邊`、`場刊`

**第一個小標之前的正文** ＝ 心得。

只寫其中幾樣、或什麼都不寫，都可以。
全空的檔案會被當成「待補」跳過，不會覆蓋既有內容。

## 檔名綁的是 ID，不是編號

`043.md` 對應的是 **ID 43**，不是 `#043`。
兩者現在是分開的：編號（`#042`）是圖鑑上的顯示序號，可以隨你重排；
ID 是永久鍵，照片、心得、打卡備份碼、分享網址全都綁它，給了就不能再動。

所以**在 Sheet 中間插一列、重排編號，這裡的檔案不會跟著錯位**。
（Sheet 還沒有 `ID` 欄的話會退回用編號，那時候才要小心。）

## 什麼該寫在這裡、什麼該留在 Sheet

| | 寫在哪 | 為什麼 |
|---|---|---|
| 心得、簡介、曲目、場刊 | **這裡** | 長內容。塞進試算表的一個儲存格既難寫也難改 —— 那就是為什麼 59 場至今一篇心得都沒有 |
| 票價、售票狀況、主視覺、系列 | 兩邊都行 | 短，但既然已經開了檔案，順手寫在一起比較不用切換 |
| 日期、地點、人物、樂團、封面 | **Sheet** | 那是結構化資料，試算表本來就擅長 |

兩邊都填的話**這裡優先** —— 這裡是後折進去的。

## 有一個更省事的入口

`npm run dev` 之後開 `/__fill`：一次一筆，旁邊放齊寫這句話需要知道的東西
（封面、日期、場館、誰來了、這是第幾次、距上一場多久）。
填曲目時還會列出已經打過的歌名，點一下就補進去 —— 寫法才會一致。

## 待補清單

先寫這幾場就夠了，不用 59 場全寫。

| ID | 活動 |
| --- | --- |
| 001 | 2018 第一次點火：Poppin'Party 首度來台 |
| 018 | 2024 RAISE A SUILEN ASIA TOUR 台北 |
| 024 | 2025 MyGO!!!!! 聲優見面會 |
| 033 | 2025 Roselia ASIA TOUR 台北公演 |
| 042 | 2026/4/11 moment / memory |
| 043 | 2026/4/12 DREAMS GO ON |
| 044 | 2026/4/25 AIMI NEW WORLD |
| 049 | 2026/8/2 Poppin'Party 見面會 |
| 051 | 2026/8/8–8/9 Ave Mujica Exitus 追加公演 |
| 052 | 2026/8/15 反田葉月 全力Melty Summer!!! |
